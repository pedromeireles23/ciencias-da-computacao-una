#include <ctype.h>
#include <stdio.h>

int main() {
    char c = 'Z';

    if (isupper(c)) {
        printf("%c é uma letra maiúscula.\n", c);
    } else {
        printf("%c não é uma letra maiúscula.\n", c);
    }

    return 0;
}